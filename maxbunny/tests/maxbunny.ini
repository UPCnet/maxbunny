# Used for rabbitMQ publishers, consumers and scripts

[main]
common = common.ini
cloudapis = cloudapis.ini
instances = instances.ini
plugins = conversations
default_domain = tests
logging_folder = /tmp

smtp_server = localhost
notify_address = noreply@localhost
notify_recipients =
